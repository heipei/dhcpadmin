<table class="sortable">
<tr><th>DNS-Name</th><th>IP-Adresse</th><th>MAC-Adresse</th><th>Aktionen</th></tr>

<% @rooms.each do |room| %>
  <% @room = room %>
  <tr><td><%= room.dns %></td><td><tt><%= room.ip %></tt></td><td><tt><%= room.mac %></tt></td>
    <td>
      <%= link_to image_tag("trash.png"), room, :confirm => "Wirklich #{room.dns} löschen?", :method => :delete %>
      <%= link_to image_tag("edit.png"), edit_room_path(room.id) %>
    </td>
  </tr>
<% end %>

</table>
